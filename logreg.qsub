#!/bin/bash -l

#$ -t 1-5
#$ -l h_rt=16:00:00
# Define the array without spaces around the equals sign
v=(2 5 13 27 59)

# Extract the element using the task ID, adjusting for zero-based indexing
a=${v[$((SGE_TASK_ID - 1))]}

# Navigate to the build directory and execute the program with the selected value of a
cd build
./lr_nag -c "$a" -w "../results_deneme/"